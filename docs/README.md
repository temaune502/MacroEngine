# TML (Task Macro Language) Documentation

Цей документ містить опис стандартної бібліотеки функцій TML, включаючи нещодавно додані можливості для керування системою та розширеної автоматизації.

## 📺 Екран (`screen`)
Об'єкт для взаємодії з монітором та аналізу зображення.

- `screen.get_brightness()` — Повертає поточний рівень яскравості (0-100).
- `screen.set_brightness(level)` — Встановлює яскравість (0-100).
- `screen.monitor_on()` — Вмикає монітор.
- `screen.monitor_off()` — Вимикає монітор.
- `screen.find_color(color, x, y, w, h, tolerance)` — Шукає колір у заданій області. Повертає координати `Vector` або `None`.
- `screen.wait_for_color(color, x, y, timeout, tolerance)` — Чекає появи кольору в точці до `timeout` секунд.
- `screen.wait_for_image(path, timeout, confidence)` — Чекає появи картинки на екрані.

## 🔊 Звук (`sound`)
Керування системним аудіо.

- `sound.get_volume()` — Отримує рівень гучності (0-100).
- `sound.set_volume(level)` — Встановлює гучність (0-100).
- `sound.mute()` — Вимикає звук.
- `sound.unmute()` — Вмикає звук.
- `sound.beep(freq, duration)` — Відтворює системний сигнал.

## ⌨️ Клавіатура (`key`)
- `key.is_pressed(K_KEY)` — Перевіряє, чи затиснута клавіша фізично в даний момент. Повертає `true/false`.
- `key.tap(key)` — Одиночне натискання.
- `key.press(key)` / `key.release(key)` — Затискання та відпускання.

## 🖱️ Миша (`mouse`)
- `mouse.is_pressed(button)` — Перевіряє, чи затиснута кнопка миші (left, right, middle).
- `mouse.move(x, y)` — Переміщує курсор.
- `mouse.click(button)` — Клік вибраною кнопкою.

## 🖼️ Оверлей (`ui`)
Керування HUD-вікном поверх усіх вікон.

- `ui.show()` / `ui.hide()` — Керування видимістю.
- `ui.set_text(id, val)` — Виведення даних.
- `ui.anchor(position)` — Прив'язка до кута екрана.
- `ui.set_color(id, color)` — Налаштування вигляду.

## ⚙️ Система (`system`)
- `system.get_keyboard_layout()` — Повертає ID поточної розкладки.
- `system.set_keyboard_layout(lang)` — Змінює розкладку ('en', 'uk', 'ru').
- `system.alert(message)` — Показує вікно з повідомленням.

---
**Порада:** Використовуйте `is_pressed` всередині `on_tick` для створення реактивних макросів, які працюють лише при затиснутих клавішах-модифікаторах.
